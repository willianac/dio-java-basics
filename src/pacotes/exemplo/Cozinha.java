package pacotes.exemplo;

// classe default = visto pelas classes do mesmo package.
class Cozinha {
  // metodo default = visto pelas classes do mesmo package.
  void metodoDefault() {
    System.out.println("esse é um metodo DEFAULT");
  }
}
